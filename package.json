{
  "name": "fly-hello-fastify-db-replay",
  "version": "1.0.0",
  "description": "A sample Fastify application that uses the fly-replay header",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "nodemon src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "dependencies": {
    "@prisma/client": "^3.12.0",
    "dotenv": "^16.0.0",
    "fastify": "^3.28.0",
    "nanoid": "^3.3.2"
  },
  "devDependencies": {
    "prisma": "^3.12.0"
  }
}
